const config = require('./knexfile').development
const db = require('knex')(config)

test('getaAll returns all Todos from the database', () => {
  throw Error('TODO: Emtpy test.')
})

test('addTodo adds a new task to the database', () => {
  throw Error('TODO: Emtpy test.')
})
